import { test } from "@playwright/test";

test.describe("Positioning", () => {
  test("should take tart positioning and viewport dimensions into account", async ({
    page,
  }) => {
    // TODO
  });
});
