import { useContextMenu } from "use-context-menu";

const menuItems = <></>;

// <begin>

// eslint-disable-next-line react-hooks/rules-of-hooks
useContextMenu(menuItems, { alignTo: "below" });

// <end>
